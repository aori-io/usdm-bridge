# Dependencies
node_modules/

# Build output (packages are built on demand / in CI before publish)
dist/
*.tsbuildinfo

# Framework build/cache output
.next/
.turbo/
out/
.vite/

# Environment files
.env
.env.local
.env.*.local

# Lockfiles from other package managers (this repo uses bun.lock)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# yalc (no longer used — kept ignored to avoid accidental re-introduction)
.yalc/
yalc.lock
